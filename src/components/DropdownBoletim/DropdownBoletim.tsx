import React from 'react';

import {
  DropdownContainer,
  OptionText,
} from './styles';

const DropdownBoletim = ({}) => {
  return (
    <DropdownContainer>
        <OptionText>Teste</OptionText>
    </DropdownContainer>
  );
};

export default DropdownBoletim;
